# MongoDB Connection String
MONGODB_URI=your_mongodb_uri_here

# NextAuth Configuration
NEXTAUTH_SECRET=your_nextauth_secret_here
NEXTAUTH_URL=http://localhost:3000

# For Vercel Deployment:
# 1. Set MONGODB_URI to your Atlas connection string.
# 2. Set NEXTAUTH_SECRET to a random string (e.g., generated via `openssl rand -base64 32`).
# 3. NEXTAUTH_URL should be your Vercel deployment URL (e.g., https://your-app.vercel.app).
